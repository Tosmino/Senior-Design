#include "mcc_generated_files/system.h"
#define DELAY 160

int main(void)
{
    int k;
    SYSTEM_Initialize();
    TRISA=0xff00;
    
    
    while (1)
    {
        PORTA=PORTA+1;
        for(k=0;k<DELAY;k++)
            Nop();
    }

    return 1;
}